

service: crowdingless

frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  region: sa-east-1
  memorySize: 256
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "s3:GetObject"
        - "s3:PutObject"
      Resource:
        - "*"



functions:
  create: 
    handler: user.create
    events:
      - http:
          path: /v1/user
          method: post
  list: 
    handler: user.list
    events:
      - http:
          path: /v1/user
          method: get
  get: 
    handler: user.get
    events:
      - http:
          path: /v1/user/{email}
          method: get
  update: 
    handler: user.update
    events:
      - http:
          path: /v1/user/{email}
          method: put
  delete: 
    handler: user.delete
    events:
      - http:
          path: /v1/user/{email}
          method: delete
  optimize:
    handler: optimize.handle
    environment: 
      bucket: bucketimagecrowdingless
    events:
      - s3:
          bucket: bucketimagecrowdingless
          event: s3:ObjectCreated:*
          rules:
            - prefix: uploads/
            - suffix: .jpg
      - s3:
          bucket: bucketimagecrowdingless
          event: s3:ObjectCreated:*
          rules:
            - prefix: uploads/
            - suffix: .png
      - s3:
          bucket: bucketimagecrowdingless
          event: s3:ObjectCreated:*
          rules:
            - prefix: uploads/
            - suffix: .jpeg
