{
  "name": "crowdingless",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "predeploy": "rm -rf node_modules/sharp && yarn add --platform=linux --arch=x64 --target=12.x sharp aws-serverless-express express cors dynamodb-toolbox",
    "deploy": "serverless deploy -v",
    "postdeploy": "rm -rf node_modules/sharp && yarn add sharp aws-serverless-express express cors dynamodb-toolbox",
    "prewindeploy": "rd /q /s node_modules & yarn add --platform=linux --arch=x64 --target=12.x sharp aws-serverless-express express cors dynamodb-toolbox",
    "windeploy": "serverless deploy -v",
    "postwindeploy": "rd /q /s node_modules & yarn add sharp aws-serverless-express express cors dynamodb-toolbox",
    "predevwindeploy": "rd /q /s node_modules & yarn add --platform=linux --arch=x64 --target=12.x sharp aws-serverless-express express cors dynamodb-toolbox",
    "devwindeploy": "serverless deploy -v --stage dev",
    "postdevwindeploy": "rd /q /s node_modules & yarn add sharp aws-serverless-express express cors dynamodb-toolbox",
    "logs": "serverless logs -f crowdingless-api --stage dev",
    "destroy": "serverless remove",
    "destroydev": "serverless remove --stage dev"
  },
  "repository": "https://github.com/joaobispo2077/crowdingless.git",
  "author": "João Bispo <vitor.vi10@hotmail.com>",
  "license": "MIT",
  "dependencies": {
    "aws-serverless-express": "^3.3.8",
    "cors": "^2.8.5",
    "dynamodb-toolbox": "^0.2.0-beta.1",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "sharp": "^0.26.3"
  }
}
